Portal Media Gallery for Odoo 16 portal users.

If you want to prevent odoo from resizing your images upon upload, you can set the the following key in yor System Parameters:

    base.image_autoresize_max_px = 0